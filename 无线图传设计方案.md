# Orange Pi 5 Pro 无线图传设计方案

## 1. 目标

在 Orange Pi 5 Pro 上完成以下实时链路：

```text
USB 全局快门摄像头
-> GStreamer MJPEG 采集
-> RKNN INT8 三核 NPU 检测
-> 绘制钢珠检测框与性能信息
-> RK3588 硬件 H.264 编码
-> 5 GHz Wi-Fi 无线传输
-> Windows/手机接收显示
```

推荐目标参数：

| 项目 | 建议值 |
|---|---|
| 检测输入 | 640x640 |
| 图传分辨率 | 640x480，后续可升级 1280x720 |
| 图传帧率 | 60 FPS |
| 视频编码 | H.264 Low Latency |
| 码率 | 4-8 Mbps |
| 网络 | 5 GHz Wi-Fi |
| 首选协议 | RTP/UDP |
| 端到端目标延迟 | 50-120 ms，需实测 |

摄像头和 NPU 可以超过 60 FPS，但多数显示器、无线终端和播放器以 60 Hz 为主。无线图传优先稳定输出 60 FPS，比强行发送 90 FPS 更实用。

## 2. 手册依据

根据《OrangePi 5 Pro RK3588S 用户手册 v1.3-1》：

- 第 11-12 页：板卡提供 Wi-Fi 5、千兆网口、USB 2.0/3.0、RK3588S、NPU 和视频相关硬件能力。
- 第 193-201 页：Linux 支持通过 `nmcli`、`nmtui` 连接 Wi-Fi。
- 第 210 页起：Linux 镜像支持通过 `create_ap` 创建 Wi-Fi 热点，支持 NAT 和 Bridge 模式。
- 第 213 页附近：`create_ap` 可通过 `--freq-band 5` 创建 5 GHz 热点。
- 第 247 页起：支持 UVC USB 摄像头，可通过 `v4l2-ctl --list-devices` 获取 `/dev/videoX`。
- 第 510 页硬件适配表：Android 系统中 GPU、NPU、VPU 均有适配记录。

手册没有直接给出 Linux 下 RK3588 硬件编码器的具体 GStreamer 命令，因此部署前必须检查系统镜像实际提供的 MPP/GStreamer 插件。

## 3. 推荐网络拓扑

### 方案 A：独立 5 GHz 路由器，推荐

```mermaid
flowchart LR
    C["USB Camera"] --> O["Orange Pi 5 Pro"]
    O -->|"5 GHz Wi-Fi RTP/UDP"| R["5 GHz Router"]
    R --> P["Windows PC / Receiver"]
```

优点：

- 无线稳定性、覆盖范围和天线性能通常优于开发板直接开热点。
- Orange Pi 只负责采集、检测、编码和发送。
- 便于电脑、手机等多个设备接入。

建议让路由器固定 5 GHz 信道，并给 Orange Pi 分配固定 DHCP 地址。

### 方案 B：Orange Pi 创建 5 GHz 热点

```mermaid
flowchart LR
    C["USB Camera"] --> O["Orange Pi 5 Pro AP"]
    O -->|"Direct 5 GHz Wi-Fi"| P["Windows PC / Phone"]
```

适合无路由器、点对点、近距离移动场景。手册给出的 `create_ap` 支持指定 5 GHz：

```bash
sudo create_ap -m nat wlan0 eth0 BallVision StrongPassword --freq-band 5
```

实际有线接口名称不一定是 `eth0`，先检查：

```bash
ip -br link
```

若无线图传不需要共享互联网，可考虑无上联网卡的热点模式，具体参数以板端 `create_ap -h` 为准。

## 4. 协议选择

| 协议 | 延迟 | 丢包处理 | 接收端 | 适用场景 |
|---|---:|---|---|---|
| RTP/UDP | 最低 | 不重传 | GStreamer/VLC/FFplay | 局域网实时操控，首选 |
| RTSP/RTP | 中低 | 取决于传输方式 | VLC/FFplay/NVR | 需要标准视频地址、多客户端 |
| SRT | 中等 | 重传、抗抖动 | FFplay/OBS | 无线干扰较大或跨公网 |
| WebRTC | 低 | 拥塞控制、NACK/FEC | 浏览器 | 手机/网页直接观看，开发复杂 |

本项目第一版推荐 RTP/UDP。它实现简单、缓存最少，也最容易测量真实端到端延迟。

## 5. 视频编码设计

### 编码位置

必须编码已经绘制检测框的 `frame`，而不是直接转发摄像头原始 MJPEG，否则接收端看不到检测结果。

正式程序建议增加独立编码队列：

```text
采集线程 -> NPU 工作队列 -> 3 个 NPU 推理线程
                           -> 最新结果
                           -> 本地显示线程
                           -> 编码/发送线程
```

编码队列大小建议为 1。新帧到达时覆盖旧帧，防止网络或编码器卡顿造成延迟持续增加。

### 硬件编码器检查

在香橙派执行：

```bash
gst-inspect-1.0 | grep -Ei "mpp|rockchip|h264enc|v4l2.*enc"
gst-inspect-1.0 mpph264enc
```

可能的编码插件名称包括：

```text
mpph264enc
rockchiph264enc
v4l2h264enc
```

以 `gst-inspect-1.0` 的实际结果为准。不要直接使用 `x264enc` 作为最终方案，它会占用 CPU，可能降低检测和图传帧率。

### 推荐 H.264 参数

```text
分辨率：640x480
帧率：60
码率：4-8 Mbps
GOP：30 或 60
B 帧：0
低延迟：开启
RTP MTU：1200-1400
接收缓存：0-50 ms
```

无线环境较差时优先降低码率到 3-4 Mbps，不要先增加播放器缓存。缓存虽然能减少卡顿，但会直接增加操控延迟。

## 6. GStreamer 发送链路

Python 中推荐使用 `appsrc` 接收已经绘框的 BGR 图像，然后硬件编码并通过 UDP 发送。

概念管线：

```text
appsrc
-> BGR raw video
-> videoconvert
-> NV12
-> RK3588 H.264 hardware encoder
-> h264parse
-> rtph264pay
-> udpsink
```

示例结构：

```bash
appsrc is-live=true block=false format=time \
! video/x-raw,format=BGR,width=640,height=480,framerate=60/1 \
! queue leaky=downstream max-size-buffers=1 \
! videoconvert \
! video/x-raw,format=NV12 \
! mpph264enc \
! h264parse config-interval=-1 \
! rtph264pay pt=96 config-interval=1 mtu=1200 \
! udpsink host=接收端IP port=5600 sync=false async=false
```

`mpph264enc` 的码率、GOP 和低延迟属性名称必须通过下面命令确认：

```bash
gst-inspect-1.0 mpph264enc
```

不同 Orange Pi 镜像中的插件版本和属性可能不同。

## 7. Windows 接收端

### GStreamer 接收

```powershell
gst-launch-1.0 -v `
  udpsrc port=5600 caps="application/x-rtp,media=video,encoding-name=H264,payload=96" `
  ! rtph264depay `
  ! h264parse `
  ! avdec_h264 `
  ! videoconvert `
  ! autovideosink sync=false
```

如果电脑支持硬件 H.264 解码，可将 `avdec_h264` 换成系统可用的硬件解码器。

### FFplay 接收

RTP 最好配合 SDP 文件。也可改用 MPEG-TS over UDP，方便 FFplay 直接打开：

```powershell
ffplay -fflags nobuffer -flags low_delay -framedrop udp://0.0.0.0:5600
```

MPEG-TS 使用方便，但相较精简 RTP 管线可能增加少量封装和缓存开销。

## 8. 带宽预算

H.264 码率为 6 Mbps 时，考虑 RTP/UDP/IP/Wi-Fi 开销，实际无线占用通常高于视频净码率：

```text
视频：约 6 Mbps
协议与无线开销：约 1-3 Mbps
建议稳定可用吞吐：至少 15-20 Mbps
```

Wi-Fi 5 的理论带宽远高于此，但实际性能受信道拥塞、天线、距离、遮挡、功率管理和热点模式影响。

不建议发送原始 BGR：

```text
640 x 480 x 3 x 60 x 8 ≈ 442 Mbps
```

即使是摄像头原始 MJPEG，也不适合直接作为最终检测图传，因为绘框后的画面已经被 OpenCV 解码和修改，需要重新编码。

## 9. 与现有检测代码的集成

现有 `camera_detect.py` 已具备：

- GStreamer 读取 `/dev/video0` 的 MJPEG 90 FPS 视频。
- 三个 RKNNLite Runtime 分别绑定三个 NPU 核。
- 只保留最新帧，队列满时丢弃旧帧。
- INT8 拆分输出模型，分别处理 boxes 与 scores。
- `CAM`、`NPU`、`E2E FPS` 和 `Latency` 指标。

新增无线图传时应遵守：

1. 不修改 NPU 推理线程结构。
2. 主线程完成绘框后，将画面非阻塞送入发送队列。
3. 编码和网络发送使用独立线程。
4. 发送队列始终只保留最新一帧。
5. 编码器或网络失败时不能阻塞检测程序。
6. 本地显示与无线发送可分别开关，便于性能比较。

建议新增配置：

```python
STREAM_ENABLED = True
STREAM_HOST = "192.168.1.100"
STREAM_PORT = 5600
STREAM_FPS = 60
STREAM_BITRATE = 6_000_000
LOCAL_DISPLAY = True
```

## 10. 端到端延迟测量

图传完成后，原有 `Lat` 只统计到本地 `cv2.imshow()` 提交，不能代表无线接收端延迟。

无线端到端延迟应测量：

```text
摄像头曝光
-> USB 采集与 MJPEG 解码
-> RKNN 推理
-> 绘框
-> H.264 编码
-> Wi-Fi 发送
-> 网络接收
-> H.264 解码
-> 接收端显示
```

推荐测试方法：

1. 用手机秒表或毫秒计时器作为拍摄目标。
2. 同一相机同时拍到真实计时器和接收端屏幕。
3. 比较两者显示时间差。
4. 连续采样 20 次，记录平均值、P95 和最大延迟。

软件时间戳只能测到各线程或网络阶段，无法完整覆盖摄像头曝光和显示器扫描。

## 11. 无线稳定性设置

建议：

- 强制使用 5 GHz，不使用拥挤的 2.4 GHz。
- 路由器固定非拥挤信道，避免自动跳频。
- 关闭 Wi-Fi 省电：

```bash
sudo iw dev wlan0 set power_save off
```

- 检查信号和链路：

```bash
iw dev wlan0 link
ip -s link show wlan0
```

- 使用 `iperf3` 先测 UDP 网络能力，再调视频：

电脑：

```bash
iperf3 -s
```

香橙派：

```bash
iperf3 -c 接收端IP -u -b 20M -t 20
```

关注丢包率和 jitter。20 Mbps UDP 测试稳定后，再发送 4-8 Mbps 视频通常比较稳妥。

## 12. 实施顺序

1. 确认 5 GHz Wi-Fi 或热点工作正常。
2. 使用 `iperf3` 测试 UDP 吞吐、丢包和 jitter。
3. 检查 RK3588 硬件 H.264 编码插件。
4. 先用测试画面完成 UDP 图传，不接入检测程序。
5. 将 `camera_detect.py` 的绘框结果送入 `appsrc`。
6. 显示本地 E2E 指标和发送队列丢帧数。
7. 测量真实无线端到端延迟。
8. 根据丢包和延迟调整码率、GOP、MTU 和接收缓存。

## 13. 推荐结论

第一版采用：

```text
5 GHz 路由器
+ 640x480@60
+ RK3588 硬件 H.264
+ 6 Mbps
+ GOP 30
+ 无 B 帧
+ RTP/UDP
+ 发送与接收端低缓存
```

这套方案与当前 USB 摄像头、RKNN INT8 三核检测程序兼容，开发量较低，也最容易实现低延迟。若后续要求浏览器直接观看，再在稳定的 RTP/UDP 版本基础上升级到 WebRTC。
